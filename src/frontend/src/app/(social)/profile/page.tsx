'use client';

import { ProfileContainer } from '@/components/social/ProfileContainer';

export default function ProfilePage() {
  return (
    <div className="min-h-screen bg-dark-background-primary">
      <ProfileContainer />
    </div>
  );
}
